var __rest = (this && this.__rest) || function (s, e) {
  var t = {};
  for (var p in s)
    if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
      t[p] = s[p];
  if (s != null && typeof Object.getOwnPropertySymbols === "function")
    for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
      if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
        t[p[i]] = s[p[i]];
    }
  return t;
};
import { h } from '@stencil/core';
const c = 'what3words-autosuggest';
export const CustomSuggestion = (_a) => {
  var { distance, units, description, value } = _a, props = __rest(_a, ["distance", "units", "description", "value"]);
  return (h("div", Object.assign({}, props), h("div", { class: `${c}-address` }, h("div", { class: `${c}-words`, "data-testid": "words-", style: { marginLeft: '16px' } }, value)), h("div", { class: `${c}-nearest-place`, "data-testid": "nearest-place" }, h("div", { class: `${c}-nearest-place-text`, "data-testid": "nearest-place-text" }, h("div", null, description ? description.trim() : ''), distance ? (h("div", { class: `${c}-nearest-place-distance`, "data-testid": "nearest-place-distance" }, distance, units)) : null))));
};
