import { Coordinates } from '@what3words/api';
export interface ThreeWordAddress {
  coordinates: Coordinates;
  country: string;
  language: string;
  map: string;
  nearestPlace: string;
  square: {
    northeast: Coordinates;
    southwest: Coordinates;
  };
  words: string;
}
export type GoogleMapsLibraries = ('drawing' | 'geometry' | 'localContext' | 'places' | 'visualization')[];
